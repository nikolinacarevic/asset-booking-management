# Backend API base URL
VITE_API_BASE_URL=http://127.0.0.1:8080/v1

# For production environment (i.e. when using make prod), 
# you need to have another env file named .env.production containing only following data
VITE_API_BASE_URL=/v1